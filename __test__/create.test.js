const { createItems, itemHolder } = require('../src/create')

describe('createItems', () => {
    test('should create an object that consists of 5 keys: id, itemName ', () => {
        
    });
    
});
